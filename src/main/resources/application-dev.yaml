spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: demo
    password: demo1234
    driver-class-name: org.postgresql.Driver
logging:
  level:
    com.sprint.mission.demo: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace